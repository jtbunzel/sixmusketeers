from django.db import models

# Create your models here.
class user(models.model)
    username = models.CharField(max_length = 30)
    password = models.CharField(max_length = 30)
    full_name = models.CharFiled(max_length = 40)
    address = models


